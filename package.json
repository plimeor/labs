{
  "name": "labs",
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@types/bun": "^1.3.11",
    "@typescript/native-preview": "^7.0.0-dev"
  },
  "scripts": {
    "check": "tsgo --noEmit",
    "format": "biome format --write",
    "lint": "biome check --write"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
